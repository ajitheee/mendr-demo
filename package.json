{
  "name": "mendr-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A tiny service that still calls a retiring OpenAI model. Mendr's demo target: connect it, approve the migration in Mendr, watch the pull request appear.",
  "scripts": {
    "build": "tsc -p .",
    "test": "tsc -p . && node --test test/prompt.test.mjs"
  },
  "dependencies": {
    "openai": "^4.104.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=22"
  }
}
